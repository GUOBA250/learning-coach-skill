# 示例对话

> 展示 skill 在不同场景下应该怎么工作。

---

## 场景一：用户说"背八股"

**用户**：背八股，下一题

**AI**：

好，下一题。先看一下当前进度：ES6 章节已背完，准备进 HTTP。

---

## 第 1 题：如何理解 OSI 七层模型？

### 🔴 必背清单（L1，脱稿）

1. 七层从下到上的顺序和名字：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。
2. 每层一句话职责 + 核心协议：网络层路由寻址（IP）、传输层进程通信（TCP/UDP）、应用层应用服务（HTTP/DNS/SMTP）。
3. 封装方向：发送方从应用层往下逐层加首部，接收方从物理层往上逐层解封。

> 🟡 L2：每层的具体协议细节看着图能说就行；⚪ L3：各层硬件设备（中继器、网桥那套）眼熟即可。

### 一、通俗讲解

OSI（Open System Interconnect）是国际标准化组织提出的一个网络分层模型，全称是"开放式通信系统互连参考模型"。它把计算机网络体系结构划分为七层，每一层实现各自的功能和协议，并完成与相邻层的接口通信。

为什么要有分层？因为网络通信太复杂了，一次性讲清楚所有细节根本不可能。分层之后，每一层只负责自己的事，层与层之间通过接口通信，各司其职。

想象你寄快递：你只需要把东西装进箱子、写地址（应用层），剩下的快递公司自己安排（传输、运输、分发）。你不需要知道快递车怎么走、分拣中心怎么操作。网络分层也是这个思路。

这七层从下到上是：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。

### 二、流程图（文字版）

```
OSI 七层模型
    │
    ├── 应用层（第七层）：为应用程序提供服务，如 HTTP、DNS、SMTP
    ├── 表示层（第六层）：数据压缩、加密、描述
    ├── 会话层（第五层）：建立、管理、终止会话
    ├── 传输层（第四层）：为两台主机进程提供通信服务，如 TCP、UDP
    ├── 网络层（第三层）：路由和寻址，如 IP
    ├── 数据链路层（第二层）：相邻节点之间传帧，如 MAC
    └── 物理层（第一层）：比特流传输，硬件相关
```

### 三、核心答案 🔴 L1（75-80 分标准版）

OSI 是国际标准化组织提出的网络分层模型，全称是"开放式通信系统互连参考模型"，把计算机网络体系结构划分为七层。每一层实现各自的功能和协议，并完成与相邻层的接口通信。

**七层从下到上**：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。

**物理层**：实现计算机节点之间比特流的透明传送，与硬件相关。**数据链路层**：在相邻节点之间传帧，包含 MAC 地址。**网络层**：负责路由和寻址，核心协议是 IP。**传输层**：为两台主机进程之间的通信提供服务，主要协议是 TCP 和 UDP。**会话层**：负责建立、管理和终止会话。**表示层**：数据压缩、加密和描述。**应用层**：为不同的网络应用提供服务，如 DNS、HTTP、SMTP。

**数据在各层之间的传输**：发送方从应用层往下，每层加自己的首部信息，封装成数据帧、比特流。接收方从物理层往上，逐层解封，还原出原始数据。

### 四、面试官可能的追问 🟡 L2

**追问 1：TCP/IP 和 OSI 有什么区别？**

TCP/IP 是四层或五层结构，把 OSI 的会话层、表示层、应用层合并成了应用层。TCP/IP 没有对网络接口层细分，只是概念性描述。TCP/IP 是实际应用的标准，OSI 是理论参考模型。

**追问 2：为什么 TCP/IP 要去掉表示层和会话层？**

因为会话层、表示层、应用层都是在应用程序内部实现的，最终产出的是一个应用数据包。应用程序之间几乎无法实现代码的抽象共享，所以 OSI 设想中的应用程序维度的分层无法实现。

**追问 3：数据在各层之间怎么传输？**

发送方从应用层往下，每层加自己的首部信息。应用层报文被传到传输层，加上传输层首部形成报文段。再传到网络层，加上网络层首部形成数据报。再传到数据链路层，加上 MAC 地址形成数据帧。最后物理层封装成比特流传输。接收方从物理层往上，逐层解封。

---

口述一遍，讲完我只给补丁、不给全文。

---

## 场景二：用户说"刷算法"

**用户**：刷算法，下一题

**AI**：

好，Hot100 下一题：76. 最小覆盖子串。

（今日配额：这是 hard 题，按 hard 日规则新题就这 1 道，任务块按 25+5+25 切开；做完再花 15 分钟闭卷重写昨天到期的两数之和。卡住超时我给提示，这道题顺延到明天复习位，不占你别的块。）

---

## 🔴 必背清单（L1，闭卷）

1. 题型信号：**连续子串 + 覆盖/包含所有目标字符** → 可变长度滑动窗口。
2. 框架：右指针扩张到 valid === need.size → 左指针收缩到不满足 → 收缩过程中记录最小窗口。
3. 模板骨架：`need/window` 两个 Map + `valid` 计数，外层 while 扩张、内层 while 收缩。
4. 复杂度：时间 O(n+m)（建 need 表 O(m)，窗口左右指针各走一遍 O(n)），空间 O(k)（字符集大小）。

> 🟡 L2：`valid--` 必须在 `window.set` 之前判断、`right-left` 是左闭右开长度——这些边界细节看着模板能改对即可，不要求逐字默。

---

## 🎯 卡哥式思路：这道题在考什么

题目：给字符串 `s` 和 `t`，在 `s` 里找一个**最短的连续子串**，要求它**包含 `t` 里所有字符**（顺序无所谓，但每个字符的数量要够）。

**和 438 字母异位词的区别**：438 是固定长度窗口，76 是可变长度窗口。

**关键词**：连续子串 + 覆盖所有字符 → 滑动窗口。

---

## 🎯 核心思路

**窗口扩张**：右指针向右走，把字符纳入窗口，直到窗口覆盖了 `t` 的所有字符。

**窗口收缩**：一旦覆盖了，左指针向右走，尽可能缩小窗口，同时保持"仍然覆盖"。缩到不能再缩了，记录这个窗口，然后再扩张。

用 `need` 记录 `t` 里每个字符需要多少个，`window` 记录当前窗口里每个字符有多少个。用 `valid` 记录有多少种字符的数量已经满足要求。

---

## 🎯 完整代码

```js
var minWindow = function(s, t) {
    const need = new Map()
    const window = new Map()

    for (const c of t) {
        need.set(c, (need.get(c) || 0) + 1)
    }

    let left = 0
    let right = 0
    let valid = 0
    let start = 0
    let len = Infinity

    while (right < s.length) {
        const c = s[right]
        right++
        if (need.has(c)) {
            window.set(c, (window.get(c) || 0) + 1)
            if (window.get(c) === need.get(c)) {
                valid++
            }
        }

        while (valid === need.size) {
            if (right - left < len) {
                start = left
                len = right - left
            }

            const d = s[left]
            left++
            if (need.has(d)) {
                if (window.get(d) === need.get(d)) {
                    valid--
                }
                window.set(d, window.get(d) - 1)
            }
        }
    }

    return len === Infinity ? '' : s.substr(start, len)
};
```

---

## 🎯 一行一行拆

- `right++` 放在 `s[right]` 之后，是为了让 `right - left` 直接表示窗口长度（左闭右开）。
- 如果 `c` 不在 `t` 里，直接跳过，不进 `window`。
- `window.get(c) === need.get(c)`：这个字符的数量**刚好**满足要求，`valid++`。注意是 `===`，不是 `>=`。
- 收缩时先记录当前窗口，再左移 `left`。**注意顺序**：先判断再减，否则判断条件永远为 false。

---

## 🎯 用例子跑一遍

`s = "ADOBECODEBANC"`, `t = "ABC"`

| right | 字符 | 操作 | window | valid | 是否收缩 | 最短 |
|---|---|---|---|---|---|---|
| 0 | A | 进 A | {A:1} | 1 | 否 | - |
| 1 | D | 不在 t，跳过 | {A:1} | 1 | 否 | - |
| 2 | O | 跳过 | {A:1} | 1 | 否 | - |
| 3 | B | 进 B | {A:1,B:1} | 2 | 否 | - |
| 4 | E | 跳过 | {A:1,B:1} | 2 | 否 | - |
| 5 | C | 进 C | {A:1,B:1,C:1} | 3 | 是 | ADOBEC (6) |
| 5 | - | 出 A | {B:1,C:1} | 2 | 停止 | ADOBEC (6) |
| ... | ... | ... | ... | ... | ... | ... |

最终结果：`BANC`，长度 4。

---

## 🎯 卡哥式易错点总结

| 易错点 | 说明 |
|---|---|
| `right++` 的位置 | 放在 `s[right]` 之后，表示左闭右开 |
| `valid++` 时机 | 只在该字符数量**刚好等于** need 时 +1，不是 ≥ |
| `valid--` 时机 | 出窗口前判断，如果当前数量刚好等于 need，说明一出就不满足了 |
| 不在 t 里的字符 | 不进 window，不参与 valid |

---

## 🎯 复杂度

- 时间：O(n + m)
- 空间：O(k)

---

## 🎯 一句话记住

> **右扩左缩：右边进窗口直到覆盖 t，然后左边往死里缩，缩到不覆盖前记录最短，再继续扩。**

---

这题先想 3-5 分钟摸题型，没眉目就直接看上面的讲解理解透，然后合上资料闭卷默写一遍，默完发我。

---

## 场景三：用户说"启动不了"

**用户**：我不想学，帮我开始

**AI**：

好，我们一步一步来。

**第 1 步**：现在，什么都不用做，就回答我一个问题——你现在的手机，是拿在手里，还是放在旁边？

（等用户回答。）

（用户回答"手里"后）

**第 2 步**：把手机放下，放到你手够不到的地方。放好了，回我一句「放好了」。

（等用户回答。）

（用户回答"好了"后）

**第 3 步**：坐起来，或者换个姿势，从躺变成坐。不用下床，靠着床头坐起来就行。坐好了，回我一句「坐好了」。

（继续一步一步，直到用户进入学习状态。）

---

## 场景四：用户说"帮我定个学习计划"

**用户**：帮我定个学习计划，我 14 天后面试前端岗，工作日每天能学 2 小时，周末 5 小时。八股只背过一点 Vue，算法刷过十几道，项目能讲流程但讲不清难点。

**AI**：

三个信息都齐了（倒计时 14 天、每日时长、三围基础），直接出计划。

---

## ⏰ 倒计时看板

- 面试日期：2026-10-05（周一）
- 距面试还有：**14 天**（今天 9/21 周一，是第 1 天 / 共 14 天）
- 可用总时长：约 **36 小时**（工作日 2h × 10 天 ＋ 周末 5h × 4 天 ＝ 40h，已扣 10% 弹性）
- 今日可学：2 小时 ＝ 3 个学习块（+复习、打卡各 10 分钟）

---

## 📊 三围诊断与时间配比

| 维度 | 当前基础 | 档位 | 时间占比 |
|---|---|---|---|
| 八股 | 只背过一点 Vue，JS / 网络 / 工程化没开始 | 弱 | 35% |
| 算法 | 刷过十几道，没成体系 | 弱 | 40% |
| 项目 | 能讲流程，难点和自己的贡献讲不深 | 中 | 25% |

**打法**：14 天重点突破——八股只背高频（JS 优先，Vue 捡你背过的加深），算法主攻 Hot100 高频题型，项目准备 3 个 STAR 故事。冷门章节直接放弃。

---

## 🗺️ 三阶段总览

| 阶段 | 日期 | 天数 | 主攻 | 关键产出 |
|---|---|---|---|---|
| 打底 / 补弱 | 9/21 - 9/27 | 7 天 | JS 高频八股一轮；Hot100 数组/哈希/链表；第 1 个项目 STAR | JS 12 题能脱稿讲；20 道题过完；STAR v1 |
| 强化 | 9/28 - 10/1 | 4 天 | Vue / HTTP / 工程化高频；双指针/滑窗/二叉树；项目难点 + 手写题 | 3 个 STAR 成型；各题型模板能默写 |
| 冲刺 | 10/2 - 10/4 | 3 天 | 错题过两遍 + 2 次模拟面试 + 项目逐字稿，**不学新内容** | 模拟面试走完两轮 |

---

## 📋 今日任务（第 1 天 · 9 月 21 日 · 周一）

| 块 | 时长 | 内容 | 材料 / 范围 | 产出标准（能验收） | 打卡 |
|---|---|---|---|---|---|
| 0 | 10 min | 复习：第一天无，直接开始 | - | - | ☐ |
| 1 | 35 min | 算法：Hot100 数组起步，1.两数之和（哈希）+ 283.移动零（双指针） | LeetCode Hot100 | 两题都能不看答案独立写出，能讲清双指针为什么不漏解 | ☐ |
| 2 | 40 min | 八股：JS 高频之王——闭包 | 用「背八股」四段式 | 能讲清闭包定义/用途/坑，能现场手写一个防抖 | ☐ |
| 3 | 30 min | 项目：挑简历上最熟的 1 个项目，写 STAR 骨架，列 3 个可挖难点 | 自己的简历 | 产出一段 200 字 STAR + 3 个难点标题 | ☐ |
| 4 | 10 min | 回顾打卡 | - | 回报完成几块、卡在哪 | ☐ |

**今日唯一目标**：两道算法能独立写出来，闭包能脱稿讲。项目骨架写不完可以顺延，但前两件必须完成。

---

## 📅 本周剩余安排

| 日期 | 主题 | 块数 |
|---|---|---|
| 9/22 周二 | 算法：哈希表 2 题；八股：this 指向 + 作用域链 | 3 |
| 9/23 周三 | 算法：滑动窗口（3.无重复最长子串）；八股：原型链 + new | 3 |
| 9/24 周四 | 算法：链表反转类（206 / 21）；八股：EventLoop + Promise | 3 |
| 9/25 周五 | 算法：本周错题重做；八股：异步专题 + 手写一道 | 3 |
| 9/26 周六 | 算法 8 题综合；JS 章节整复习；细化第 1 个 STAR | 7 |
| 9/27 周日 | 第 1 周自测（里程碑①）；Vue 响应式高频起步；第 2 个 STAR | 7 |

（每天只细排当天。想提前看明天的细节，说"排明天的任务"。）

---

## 🚩 里程碑检查点

- **第 7 天（9/27）**：JS 高频 12 题能脱稿讲；数组/哈希/链表约 20 题过完且能默写；第 1 个项目 STAR v1。
- **第 11 天（10/1）**：双指针/滑窗/链表/二叉树模板能默写；3 个 STAR 故事成型。
- **第 14 天（10/4）**：完成 2 次模拟面试；所有错题过两遍；项目逐字稿背熟。

---

## 🔧 计划调整规则

- 完成率 ≥90% 正常排；50%-90% 不补欠债、照常排。
- <50% 或连续 2 天没完成：次日减半，只保 1 复习块 + 1 个最高优先级 🔴 块（算法每天至少 1 道的底线不破）；连续 3 天 <50% 重算总账。
- 欠债只顺延一次，再没完成直接砍；日期变了重算重排。

---

学完今天回来打卡：**完成了几块、卡在哪、块负荷（🔵/🟢/🟠）、疲劳度**，我按完成率和负荷调整后面的计划。现在从块 1 开始——先做两数之和，写完发我，我帮你批。

### 4B. 第二天打卡：只完成 1/4 → 减半日 + 排明天

**用户**：今天只写了两数之和，八股没来得及看，加班了。卡点：两数之和哈希解法我能写，但被追问"重复元素怎么办"有点懵。

**AI**：

收到，完成 1/4（25%）。错题本现在 2 个点：昨天背闭包卡过的"闭包代价：内存不释放"（🔴 L1，今天没来得及清、顺延明天）＋刚说的"重复元素怎么办"（🟡 L2，今天入库），明天复习块一起过。

按规则 <50%，明天是**减半日**——不批评、不补债，昨天欠的 3 块不顺延（只顺延一次的额度留给今天的内容），明天只排 2 块，完成就是胜利。

## 📌 计划状态（9/22 更新）

- 倒计时：剩 13 天 ｜ 第 2 天/共 14 天 ｜ 阶段：打底
- 今日：计划 4 块 / 完成 1 块 ｜ 欠债累计 3 块（已砍，不滚到明天）
- 连续未完成：1 天 ｜ 本周完成率：25%
- 错题本：库存 2 个（🔴 1 个）｜ 明天到期 2 个
- 明天安排：2 块（复习 1 + 八股 🔴 1），主题：JS 类型 + 闭包

## 📋 明日任务（第 3 天 · 9 月 23 日 · 周三 · 减半日）

| 块 | 时长 | 内容 | 材料 / 范围 | 产出标准 | 打卡 |
|---|---|---|---|---|---|
| 0 | 15 min | 复习：错题本到期 2 个（🔴 闭包代价：内存不释放、🟡 两数之和重复元素） | 错题本表 | 脱稿口述，2/1/0 评级 | ☐ |
| 1 | 30 min | 八股 🔴：JS 数据类型 + 闭包（只学这一个主题） | 必背清单 | 合上材料脱稿讲出 🔴 清单 | ☐ |

**今日唯一目标**：块 0 两个点都拿到 2 分。算法今天不断：地铁上用手机看一道滑窗题的思路即可，不用写。

（如果明天再低于 50%，就是连续 2 天，我们不硬扛——重算总账，把"每天 2 小时"改成"工作日 1 小时 + 周末补"的真实版本。）

---

## 场景五：背八股：先给必背清单，口述后只打补丁不重写

**用户**：背八股，讲 JS 有哪些数据类型

**AI**：

## 第 1 题：JS 有哪些数据类型？

### 🔴 必背清单（L1，脱稿）

1. 8 种类型：7 种原始类型（含 Symbol、BigInt）＋ Object。
2. 原始类型放栈、引用类型放堆，**以及为什么**。
3. 类型判断三件套：`typeof` / `instanceof` / `Object.prototype.toString.call()`。

> 🟡 L2：具体 API 写法看着代码能认就行；⚪ L3：各类型的二进制存储细节，眼熟即可。

（随后按四段式输出，此处省略。讲完后——）

合上材料，对着「🔴 必背清单」口述一遍，讲完发我。

---

**用户**：（口述完。分类大体说对了，但漏了 BigInt、Symbol 的用途、函数算什么、栈堆只说结论没说原因；`Array.isArray` 拼成了 `isarray`。）

**AI**：

分类框架和 typeof 的局限都说到了，结构没乱。**你刚讲的这版就是正式版，一个字不动**，只拧 5 颗螺丝：

| # | 类型 | 拧在哪 | 补丁内容 | 为什么 |
|---|---|---|---|---|
| 1 | 📌 | 说到 BigInt 时 | "写法是数字加 n，比如 100n，解决超过 2^53-1 的精度丢失" | 🔴 新类型高频点，漏了分类不完整 |
| 2 | 📌 | 说到 Symbol 时 | "表示独一无二的值，常用来做对象 key 防命名冲突" | 🔴 只报名字答不出用途，等于没背 |
| 3 | 📌 | 说到引用类型时 | "函数本质也是对象，只是 typeof 对它特判返回 function" | 面试官爱追问"函数算什么类型" |
| 4 | 📌 | 堆栈那段后面 | 补为什么："原始类型大小固定、随作用域释放所以放栈；对象大小不固定、会被多处共享所以放堆，交给 GC" | 🔴 只说结论不说 why，一追就穿 |
| 5 | 🔁 | 类型判断结尾 | 换成 `Array.isArray`（isArray 是一个词）；再补万能方案 `Object.prototype.toString.call()` | 拼写不扣你分，但万能判断是 🔴 必须有 |

没有全文重写版——你刚搭好的记忆再看一篇 90% 相似的稿子只会互相干扰。两步：**补丁读一遍 → 合上手机，用你的原版加这 5 条再大声讲一遍**，3 分钟以内，讲完回我。想要全文稿再说"给我全文"。

---

## 场景六：全流程模拟面试

**用户**：模拟面试

**AI**：

开始前确认三件事：① 目标岗位？② 项目深挖用哪个项目（得是你已经读过、有 🔴 L1 清单的）？③ 全流程（约 50 分钟）还是单科加练（15 分钟）？

**用户**：前端，用 MiniVue 那个，全流程

**AI**：

# 🎤 模拟面试 · 第 1 次（全流程）

- 目标岗位：前端
- 流程：自我介绍 → 八股 3 题 → 算法 1 题 → 项目深挖（MiniVue）→ 反问
- 规则：脱稿、一次一题；卡住 10 秒我会给一次提示并记录；中途随时喊"停"。

准备好了就开始，先用 2 分钟做个自我介绍。

**用户**：我叫 xx，学校 xx，学过前端……（2 分 10 秒，报了技术栈流水账，没说项目和匹配点）

**AI**：

（自我介绍超时、无匹配点，记录：1 分。）

好，八股第一题：讲一下闭包。

**用户**：闭包就是函数里面套函数，里面的函数能访问外面的变量。我平时防抖节流里用过。

**AI**：

你刚没提到闭包的代价，展开说说：什么场景下闭包会带来问题？

**用户**：嗯……（沉默）……是不是变量不会释放？循环里用 var 会有问题，但具体为什么我记不清了。

**AI**：

（记录：第 1 题 1 分，经提示，漏"内存占用 + 循环 var 共享变量"的机制。）

下一题：……（八股第 2、3 题、算法题、项目深挖、反问环节按同节奏进行，AI 每次只发一个问题，全程不讲课。）

---

（50 分钟后，面试结束。）

**AI**：

# 📋 模拟面试复盘

## 1. 评分表

| 环节 | 得分 | 得分率 | 诊断 |
|---|---|---|---|
| 自我介绍 | 1/2 | 50% | 超时 10 秒，技术栈流水账，没有项目和匹配点 |
| 八股 | 4/6 | 67% | 主干都知道，一追"代价/为什么"就漏 🔴 |
| 算法 | 1/2 | 50% | 双指针思路对，边界条件改了两版才过，复杂度没主动说 |
| 项目深挖 | 4/6 | 67% | MiniVue 响应式主流程讲得顺；被问"为什么用 Proxy 不用 defineProperty"时只答了一半 |
| 反问 | 2/2 | 100% | 问了团队技术栈和 Code Review 机制，质量高 |
| **合计** | **12/18** | **67%** | |

**直觉判断**：以现在的状态去面，**边缘**——一面大概率过，二面深挖会丢分。短板集中在"知道是什么、答不出为什么"。

## 2. 最大的 3 个问题

1. **答八股只给结论不给代价**
   - 现象：闭包、Promise 都能下定义，一问"什么场景出问题"就卡住。
   - 为什么致命：面试官区分背题和理解，全靠这一追。
   - 怎么练：每道 🔴 八股的必背清单里加一条"代价/坑"，背的时候连这条一起口述。
2. **算法不说复杂度、边界靠试**
   - 现象：写完直接交，不分析复杂度；`left/right` 相遇条件改错两次。
   - 为什么致命：中等以上题这两项占一半评分。
   - 怎么练：以后每题写完固定说两句"复杂度 x，边界是空数组和单元素"。
3. **自我介绍没有钩子**
   - 现象：2 分钟报菜名，面试官没有可接话的点。
   - 为什么致命：项目深挖的方向本来该由你自己开场埋好。
   - 怎么练：改成"一句话定位 + 主项目一句话亮点 + 为什么投这个岗"，压到 90 秒。

## 3. 补丁（全场 4 条）

| # | 类型 | 拧在哪 | 补丁内容 | 为什么 |
|---|---|---|---|---|
| 1 | 📌 | 闭包答到"防抖里用过"之后 | "代价是外层变量一直被引用、不释放；循环里 var 共享同一个变量，所以要 let 或 IIFE" | 🔴 一追代价就漏，本场已丢分 |
| 2 | 📌 | 项目答 Proxy 时，补完"能监听数组"之后 | "再加一句：defineProperty 要递归遍历所有 key，Proxy 是懒代理，访问到才代理，性能更好" | 对比题只答一半 |
| 3 | 📌 | 以后每道算法写完 | 固定动作：主动报时间/空间复杂度 + 点出两个边界 | 本场没报，直接扣到 1 分 |
| 4 | 🔁 | 自我介绍开头那句 | 删掉"我学过 html/css/js/vue…"流水账，换成"我主要做 Vue 技术栈，最近在手写一个 MiniVue 理解响应式和编译" | 流水账无钩子 |

## 4. 本次新入错题本（明天到期，第 1 档；下次模拟开头先抽未出库的）

| 点（最小颗粒） | 维度 | 级别 | 丢的 L1 点 | 本次得分 |
|---|---|---|---|---|
| 闭包的代价与循环 var | 八股 | 🔴 | 内存不释放 + 共享变量机制 | 1 |
| Proxy vs defineProperty 完整对比 | 项目 | 🔴 | 懒代理 / 全量递归 / 数组 | 1 |
| 无重复字符的最长子串 | 算法 | 🔴 | 边界条件 + 复杂度主动说明 | 1 |

## 5. 下一步动作

1. 今天：4 条补丁读一遍，合上材料把这三个丢分点重讲一次，明天先清错题本。
2. 本周：已背过的 12 道 🔴 八股，每道补"代价/坑"一条，进必背清单。
3. 算法固定收尾动作（补丁 3），明天刷题开始执行。
4. 自我介绍改到 90 秒，明天找我过一遍。
5. 第 2 次模拟安排在考前 3 天，开头先复测错题本里今天入库、还没出库的点。

---

补丁读一遍，合上材料把这几道丢分题重讲一次，讲完告诉我。

---

## 场景七：错题本滚动复习（八股为重）

### 7A. 打补丁当天：自动入库

（接场景五：JS 数据类型题打完 5 条补丁、用户加补丁重讲完——）

**AI**：

顺了。这题里你卡过的 4 个点已登记错题本，明天学新题前先过：

| 点（维度·最小颗粒） | 级别 | 当前档 | 下次复习 | 战绩（最近） |
|---|---|---|---|---|
| 八股·BigInt：写法 100n + 解决 2^53-1 精度 | 🔴 | 1（1 天） | 9/22 | 新入 |
| 八股·Symbol 用途：独一无二的值做 key | 🔴 | 1（1 天） | 9/22 | 新入 |
| 八股·函数本质是对象、typeof 特判 | 🟡 | 1（1 天） | 9/22 | 新入 |
| 八股·栈/堆分配的原因（固定大小/共享/GC） | 🔴 | 1（1 天） | 9/22 | 新入 |

（`Array.isArray` 拼写是 🟡 小偏差，你已经自己改对，不入库。）

### 7B. 第二天：每日复习，2/1/0 评级调度

**用户**：复习

**AI**：

# 📒 错题本复习 · 9 月 22 日

今天到期 4 个点（🔴 3 / 🟡 1），约 8 分钟。合上材料，一次一个。

**第 1 个（八股 🔴 · 第 1 档）**：脱稿讲 BigInt——写法和它解决什么问题。

**用户**：BigInt 是数字后面加 n，比如 100n，用来表示超大整数，普通 number 超过 2^53-1 就会精度丢失。

**AI**：

2 分，升第 2 档（3 天后 9/25）。下一个——

**第 2 个（八股 🔴 · 第 1 档）**：Symbol 是干嘛的？

**用户**：Symbol 表示……独一无二的值？可以当对象的 key，防止命名冲突。对了还有遍历的时候不会被 Object.keys 枚举到。

**AI**：

2 分（枚举性是加分项），升 9/25。下一个——

**第 3 个（八股 🔴 · 第 1 档）**：原始类型放栈、对象放堆，为什么这么分？

**用户**：因为原始类型……占的空间小？对象比较大放堆里。

**AI**：

1 分留档，9/23 原档再来。差的不是"大小"是这三个词：**大小是否固定、随不随作用域释放、会不会被多处共享**——对象交给 GC。给你半句钩子，不用重背，明天用你自己的话把这三个词串出来。

第 4 个（🟡 函数是对象）2 分略。

# ✅ 今日复习战报

- 2 分 3 个（升 3 天档）/ 1 分 1 个（留档明天）/ 0 分 0
- 出库 0，新进 0，钉子 0
- 📊 库存水位：库存 5 个（🔴 2 / 🟡 3 / 🔁 0）｜ 今日入 0 出 4 ｜ 连续 0 天入>出
- 明天到期 1 个（🔴 栈堆原因），复习完再开新题。

### 7C. 钉子题：两次 0 分后拆小 / 判生死

（栈堆原因这个点：9/23 又没串出来 → 0 分打回；9/24 依然只说得出"大小"——）

**AI**：

### 🔁 钉子题：栈/堆分配的原因

这个点已经卡了两次（1 分、0 分、0 分），不再整句硬背。拆成两个最小事实，一次只背第 1 个：

1. **今天只背这句**："原始类型大小固定、随作用域结束释放 → 放栈。"（就这半句，合上材料说三遍，能脱口而出就行。）
2. 明天加第 2 句："对象大小不固定、会被多处共享、交给 GC → 放堆。"

两句各自滚顺了，再合起来。重置回第 1 档，明天先复查第 1 句。

（如果拆完两次还是 0 分，就走"判生死"：这是超高频考点不能放，换通道——画一张"栈帧进出 + 堆里对象被引用"的图，用图记；低频边角才允许降 L3 放弃。）

### 7D. 出库不是毕业：周末回测

**AI**：

本周出库 6 个点，周末抽 20%（2 个）回测：BigInt、闭包代价。脱稿讲，回测 0/1 的重新进第 1 档。

（算法点的复习形态不同：到期是**闭卷重写**，写完主动报复杂度；项目点对着物证讲因果链，不考脱稿。三者共用一本错题本、同一套 1/3/7/15 调度。）

### 7E. 连续两天入 > 出 → 止血日

（接下来两天冲新章节，昨天入 5 出 2、今天入 4 出 1。今天的战报水位行——）

**AI**：

📊 库存水位：库存 11 个（🔴 5 / 🟡 6）｜ 今日入 4 出 1 ｜ **连续 2 天入>出**

库存涨得比消化快，明天按规则排**止血日**——这是计划内的缓冲，不是欠债：

> 🚑 止血日（明天）：今日不学新。
> 上午：清到期 + 这两天新进的 9 个点批量过补丁，八股里连续无印象的点直接走回炉分诊；
> 下午：算法到期 2 道闭卷重写。
> 目标：一天结束做到"出 ≥ 入"，后天恢复正常排新。

（另：🔴 到了 5 个还没破 12，暂时不做判生死盘点；等超过 12 我会拉你一起盘低频点。）

---

## 场景八：八股连续"完全无印象" → 痕迹分诊 → 回炉三段式

（复习场景。今天到期的是 EventLoop 主题 3 个点，用户前两个都答不上。）

**用户**：第一个……不知道。第二个也完全没印象，跟没学过一样。

**AI**：

等一下，先不往下测第三个。前两个点你是"话到嘴边但说不出"，还是"跟从没见过一样"？

**用户**：跟没见过一样，一脸懵。

**AI**：

好，这是**记忆痕迹磨光**，不是提取卡壳——继续闭卷测第三个只是在重复挫败，按规则触发**回炉**（同一主题连续 2 点无痕迹，今天回炉 1 次、15 分钟封顶）。补丁协议这时候不用，它治的是"有印象但说不出"。

**① 回炉 15**：我用 3 句话重讲 EventLoop 的核心，你听完立刻接话复述——
1. JS 单线程，所有任务进队列；同步代码先全部跑完；
2. 同步清空后，先清**微任务**队列（Promise.then），再取**一个**宏任务（setTimeout）执行；
3. 每执行完一个宏任务，再回头清空一次微任务，如此循环；渲染发生在宏任务之间。

来，用你自己的话把这三句复述一遍，卡了我提示。

**用户**：（复述，基本完整，漏了"宏任务之间渲染"，教练补一句后再复述到位。）

**AI**：

**② 热测 20**：刚回炉的微任务/宏任务顺序，闭卷讲一遍（这题 1 分就算过）；再来一个旧错题——闭包的代价。

（用户：EventLoop 顺序讲对得 1 分；闭包代价 2 分升档。）

**③ 冷枪 10**：抽个大前天的——浏览器输入 URL 到页面显示，说三步就行。（用户答出 DNS、TCP、渲染，2 分。）

### 📒 回炉点入库

| 点（维度·最小颗粒） | 级别 | 当前档 | 下次复习 | 战绩（最近） |
|---|---|---|---|---|
| 八股·EventLoop：同步→微任务→宏任务循环（含渲染时机） | 🟡 | 1（1 天） | 9/25 | 回炉：热测 1、冷枪 2 |

回炉点明天热测仍 0 分才升 🔴 钉子；今天的回炉到此为止，不重讲第二遍。

---

## 场景九：读项目：任务先行六步 + 埋点判生死

（项目模式统一走任务先行。这块学 vite 的 proxy 配置，只做一个可观测点。）

**AI**：

**① 词汇卡 2min**：`resolve - 把路径拼成绝对路径` ｜ `plugin - 给构建流程挂钩子` ｜ `proxy - dev-server 把请求转发到后端`。先不展开，做完任务再收口。

**② 任务书**：打开 `vite.config.js` L18-L24 的 `server.proxy` 块，把 `/api` 改成 `/api2`，启动项目点一次登录；**怎么算赢**：控制台里登录请求 404、终端能看到转发记录，就说明这条配置真的在管事。

**③ 先预测**：动手前先说一句——你猜改完后，发出的请求 URL 会变成什么？

**用户**：我猜会请求 `/api2/login`，后端没这个路径所以 404。

**AI**：预测记下了。**④ 动手验证**：自己改、自己点，卡住超 5 分钟我再给提示；回来告诉我结果。

**用户**：真的 404 了！而且终端打印了一行转发记录。

**AI**：

**⑤ 机制收口 3min**（只讲你刚踩的）：proxy 的 🔴 L1 一句话——"**开发环境下浏览器只跟 dev-server 通信，server.proxy 按前缀把请求转发到真实后端，用来绕开跨域**"。你刚改前缀就 404，正是因为转发规则是按前缀匹配的。别背书——就对着刚才的控制台 404 和终端转发记录这两样物证，把因果链讲一遍：浏览器请求了谁、dev-server 看到什么前缀、做了什么、后端为什么回 404。（用户对着物证讲通，2 分，不入库。）

**⑥ 词汇收尾**：`proxy ｜ dev-server 按前缀转发请求到后端绕开跨域 ｜ 在 vite.config.js L18 亲手改过前缀`，现场再过一遍就够，不建表存档；`resolve`、`plugin` 今天没亲手用，不收。

（三天后，"跨文件数据流"类项目钉子复测——）

**AI**：这个点已连吃两个 0 分，不再脱稿空讲。**通道：埋点追时序**——在 `sider-view.vue` L92 回调首行、`schema.js` L20 和 L72 各埋一条 `console.log`，点一次菜单，把控制台输出按顺序贴给我，并自己画一张时序图。

**用户**：（贴出 3 行 log 的顺序，画出"组件读取 → schema 解析 → 回调写回"时序图。）

**AI**：对着你自己画的图讲一遍数据怎么接力。……讲通了，**2 分，重置第 1 档**。这就是物证复测——你产出过的证据比空口背书可靠。

---

## 场景十：排计划四自检 + 打卡负荷次日联动

（背景：用户目标每日 4h。教练初排 D3：错题 → 八股 → 算法（只复习 #54、#73 两个钉子，0 新题，50min）→ 项目（补 mock + 表格出数 + 编辑回显 + 详情 4 项 + 验 mainKey，一块全包），合计 2.5h，项目排最后。）

**AI**（输出任务表前先跑四条自检，把校验过程亮给用户）：

> **⏱ 时长校验**：目标每日 4h（240min），初排合计 150min ≈ 2.5h，**缺口 90min > 30min，必须补**——算法按复习优先 + 4 道新 medium 重排、项目块拆两天后多出的时间给弱项八股加 1 个新题块。
> **🔋 精力曲线**：原顺序把最难的项目块排在精力最差的最后，调整为 复习 → 项目 → 算法 → 八股 → 打卡。
> **🧮 算法自检**：原排 0 道新题且非 hard 日 = 排程事故。先排 **30min 到期默写**（#54、#73 闭卷默写，不问 DP 五部曲），默完再排"**今日算法：4 道新 medium**"，时长 = 30 + 4×12 = **78min**（每道想 3-5 分钟摸题型 → 看题解理解 → 合上默写）。
> **🔬 项目粒度**：一块 5 个可观测点太大，本块只做"**表格出数**"（改 `mock/table.js` L8 加 3 条数据 → 页面表格渲染出 3 行 = 赢）；"编辑回显"明天做，详情与 mainKey 顺延到后天。

**🔋 今日精力曲线：高 → 低，对应块顺序：复习 → 项目 → 算法 → 八股 → 打卡**

| 块 | 时长 | 内容 |
|---|---|---|
| 0 | 15min | 复习：错题本到期 6 个 |
| 1 | 40min | 项目：只做"表格出数"一个点 |
| 2 | 78min | 算法：30min 到期默写（#54/#73）+ 4 道新 medium |
| 3 | 60min | 八股：新题 2 道 + 冷枪 10min |
| 4 | 10min | 回顾打卡 |

合计 203min……仍差 37min，八股再加 1 道新题（35min）→ 238min，缺口 2min，在弹性内，**过**。

（当天晚上打卡——）

**用户**：完成 5 块，项目那块 🟠 吃力，表格数据出来了但找 mock 文件找了半天；算法 🟢、八股 🔵 太松。

**AI**：

收到。#54、#73 的到期默写情况单独说一下——复习段我没问你 DP 五部曲，直接让你默代码是故意的：框架是理解用的、不背，复习只做模板提取；新题也是想 3-5 分钟没眉目就看题解、理解完合上默写，今天 4 道都默出来了吗？

**🔧 基于今日负荷，明日调整：**
- **项目块**（今日 🟠）：拆小 + 降难度——明天"编辑回显"只做**输入框回填 1 个字段**，任务书写清文件行号，不再一块多目标；
- **八股块**（今日 🔵，已是连续第 2 天 🔵）：加量——新题 2 → 3 道；
- 算法 🟢，维持 30min 复习 + 4 道新 medium。

明日任务表已按这个调整重排，开头同样先过四条自检。