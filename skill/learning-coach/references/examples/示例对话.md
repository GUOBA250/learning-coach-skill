# 示例对话

> 展示 skill 在不同场景下应该怎么工作。

---

## 场景一：用户说"背八股"

**用户**：背八股，下一题

**AI**：

好，下一题。先看一下当前进度：ES6 章节已背完，准备进 HTTP。

---

## 第 1 题：如何理解 OSI 七层模型？

### 一、通俗讲解

OSI（Open System Interconnect）是国际标准化组织提出的一个网络分层模型，全称是"开放式通信系统互连参考模型"。它把计算机网络体系结构划分为七层，每一层实现各自的功能和协议，并完成与相邻层的接口通信。

为什么要有分层？因为网络通信太复杂了，一次性讲清楚所有细节根本不可能。分层之后，每一层只负责自己的事，层与层之间通过接口通信，各司其职。

想象你寄快递：你只需要把东西装进箱子、写地址（应用层），剩下的快递公司自己安排（传输、运输、分发）。你不需要知道快递车怎么走、分拣中心怎么操作。网络分层也是这个思路。

这七层从下到上是：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。

### 二、流程图（文字版）

```
OSI 七层模型
    │
    ├── 应用层（第七层）：为应用程序提供服务，如 HTTP、DNS、SMTP
    ├── 表示层（第六层）：数据压缩、加密、描述
    ├── 会话层（第五层）：建立、管理、终止会话
    ├── 传输层（第四层）：为两台主机进程提供通信服务，如 TCP、UDP
    ├── 网络层（第三层）：路由和寻址，如 IP
    ├── 数据链路层（第二层）：相邻节点之间传帧，如 MAC
    └── 物理层（第一层）：比特流传输，硬件相关
```

### 三、核心答案（75-80 分标准版）

OSI 是国际标准化组织提出的网络分层模型，全称是"开放式通信系统互连参考模型"，把计算机网络体系结构划分为七层。每一层实现各自的功能和协议，并完成与相邻层的接口通信。

**七层从下到上**：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。

**物理层**：实现计算机节点之间比特流的透明传送，与硬件相关。**数据链路层**：在相邻节点之间传帧，包含 MAC 地址。**网络层**：负责路由和寻址，核心协议是 IP。**传输层**：为两台主机进程之间的通信提供服务，主要协议是 TCP 和 UDP。**会话层**：负责建立、管理和终止会话。**表示层**：数据压缩、加密和描述。**应用层**：为不同的网络应用提供服务，如 DNS、HTTP、SMTP。

**数据在各层之间的传输**：发送方从应用层往下，每层加自己的首部信息，封装成数据帧、比特流。接收方从物理层往上，逐层解封，还原出原始数据。

### 四、面试官可能的追问

**追问 1：TCP/IP 和 OSI 有什么区别？**

TCP/IP 是四层或五层结构，把 OSI 的会话层、表示层、应用层合并成了应用层。TCP/IP 没有对网络接口层细分，只是概念性描述。TCP/IP 是实际应用的标准，OSI 是理论参考模型。

**追问 2：为什么 TCP/IP 要去掉表示层和会话层？**

因为会话层、表示层、应用层都是在应用程序内部实现的，最终产出的是一个应用数据包。应用程序之间几乎无法实现代码的抽象共享，所以 OSI 设想中的应用程序维度的分层无法实现。

**追问 3：数据在各层之间怎么传输？**

发送方从应用层往下，每层加自己的首部信息。应用层报文被传到传输层，加上传输层首部形成报文段。再传到网络层，加上网络层首部形成数据报。再传到数据链路层，加上 MAC 地址形成数据帧。最后物理层封装成比特流传输。接收方从物理层往上，逐层解封。

---

口述一遍，我帮你校正。

---

## 场景二：用户说"刷算法"

**用户**：刷算法，下一题

**AI**：

好，Hot100 下一题：76. 最小覆盖子串。

---

## 🎯 卡哥式思路：这道题在考什么

题目：给字符串 `s` 和 `t`，在 `s` 里找一个**最短的连续子串**，要求它**包含 `t` 里所有字符**（顺序无所谓，但每个字符的数量要够）。

**和 438 字母异位词的区别**：438 是固定长度窗口，76 是可变长度窗口。

**关键词**：连续子串 + 覆盖所有字符 → 滑动窗口。

---

## 🎯 核心思路

**窗口扩张**：右指针向右走，把字符纳入窗口，直到窗口覆盖了 `t` 的所有字符。

**窗口收缩**：一旦覆盖了，左指针向右走，尽可能缩小窗口，同时保持"仍然覆盖"。缩到不能再缩了，记录这个窗口，然后再扩张。

用 `need` 记录 `t` 里每个字符需要多少个，`window` 记录当前窗口里每个字符有多少个。用 `valid` 记录有多少种字符的数量已经满足要求。

---

## 🎯 完整代码

```js
var minWindow = function(s, t) {
    const need = new Map()
    const window = new Map()

    for (const c of t) {
        need.set(c, (need.get(c) || 0) + 1)
    }

    let left = 0
    let right = 0
    let valid = 0
    let start = 0
    let len = Infinity

    while (right < s.length) {
        const c = s[right]
        right++
        if (need.has(c)) {
            window.set(c, (window.get(c) || 0) + 1)
            if (window.get(c) === need.get(c)) {
                valid++
            }
        }

        while (valid === need.size) {
            if (right - left < len) {
                start = left
                len = right - left
            }

            const d = s[left]
            left++
            if (need.has(d)) {
                if (window.get(d) === need.get(d)) {
                    valid--
                }
                window.set(d, window.get(d) - 1)
            }
        }
    }

    return len === Infinity ? '' : s.substr(start, len)
};
```

---

## 🎯 一行一行拆

- `right++` 放在 `s[right]` 之后，是为了让 `right - left` 直接表示窗口长度（左闭右开）。
- 如果 `c` 不在 `t` 里，直接跳过，不进 `window`。
- `window.get(c) === need.get(c)`：这个字符的数量**刚好**满足要求，`valid++`。注意是 `===`，不是 `>=`。
- 收缩时先记录当前窗口，再左移 `left`。**注意顺序**：先判断再减，否则判断条件永远为 false。

---

## 🎯 用例子跑一遍

`s = "ADOBECODEBANC"`, `t = "ABC"`

| right | 字符 | 操作 | window | valid | 是否收缩 | 最短 |
|---|---|---|---|---|---|---|
| 0 | A | 进 A | {A:1} | 1 | 否 | - |
| 1 | D | 不在 t，跳过 | {A:1} | 1 | 否 | - |
| 2 | O | 跳过 | {A:1} | 1 | 否 | - |
| 3 | B | 进 B | {A:1,B:1} | 2 | 否 | - |
| 4 | E | 跳过 | {A:1,B:1} | 2 | 否 | - |
| 5 | C | 进 C | {A:1,B:1,C:1} | 3 | 是 | ADOBEC (6) |
| 5 | - | 出 A | {B:1,C:1} | 2 | 停止 | ADOBEC (6) |
| ... | ... | ... | ... | ... | ... | ... |

最终结果：`BANC`，长度 4。

---

## 🎯 卡哥式易错点总结

| 易错点 | 说明 |
|---|---|
| `right++` 的位置 | 放在 `s[right]` 之后，表示左闭右开 |
| `valid++` 时机 | 只在该字符数量**刚好等于** need 时 +1，不是 ≥ |
| `valid--` 时机 | 出窗口前判断，如果当前数量刚好等于 need，说明一出就不满足了 |
| 不在 t 里的字符 | 不进 window，不参与 valid |

---

## 🎯 复杂度

- 时间：O(n + m)
- 空间：O(k)

---

## 🎯 一句话记住

> **右扩左缩：右边进窗口直到覆盖 t，然后左边往死里缩，缩到不覆盖前记录最短，再继续扩。**

---

这题你先自己写一遍，写完发我。

---

## 场景三：用户说"读项目"

**用户**：继续读 MiniVue，讲 parse.ts

**AI**：

好，进 parse.ts。

---

## 第 0 块：函数地图

| 函数 | 作用 | 依赖谁 |
|---|---|---|
| `createParserContext` | 把模板字符串装进 context 对象 | 无 |
| `advanceBy` | 从头部切掉已解析的字符 | 无 |
| `isEnd` | 判断是否解析到头了 | 无 |
| `parseNode` | 看开头字符，分发给具体解析函数 | 下面三个 |
| `parseText` | 解析纯文本 | `advanceBy` |
| `parseInterpolation` | 解析 `{{ }}` | `advanceBy` |
| `parseTag` | 解析 `<div>` 或 `</div>` | `advanceBy` |
| `parseElement` | 解析完整元素 `<div>...</div>` | `parseTag`、`parseChildren` |
| `parseChildren` | 循环解析所有子节点 | `isEnd`、`parseNode` |
| `createRoot` | 把节点数组包成 ROOT 节点 | 无 |
| `baseParse` | 入口 | 上面全部 |

---

## 第 1 块：`createParserContext` + `advanceBy`

### 为什么要写这两个函数？

`createParserContext` 把字符串装进一个对象里，属性名叫 `source`。因为解析过程中要不断修改 `source`，用对象包着方便共享和修改。

`advanceBy` 是"从头部砍掉 length 个字符"。解析器一直在往前走，每次消费掉已经解析的部分。

```ts
function createParserContext(content: string) {
    return {
        source: content
    }
}

function advanceBy(context: any, length: number) {
    context.source = context.source.slice(length)
}
```

**第 1 行**：定义函数，接收模板字符串 `content`。

**第 2 行**：返回一个对象。

**第 3 行**：对象的属性叫 `source`，值就是传进来的 `content`。

**大白话**：就是把字符串装进一个对象里。

---

### 用例子跑一遍

模板 `<div>hello</div>`

**初始**：`context = { source: '<div>hello</div>' }`

**解析完 `<div>` 之后**：`context = { source: 'hello</div>' }`

**解析完 `hello` 之后**：`context = { source: '</div>' }`

**解析完 `</div>` 之后**：`context = { source: '' }`

---

## 📝 复习题

**Q1：`createParserContext` 是干什么的？**

A：把模板字符串装进一个对象，属性叫 `source`。

**Q2：`advanceBy` 是干什么的？**

A：从 `source` 头部砍掉 length 个字符。

**Q3：为什么用对象包着 source，而不是直接传字符串？**

A：因为解析过程中要不断修改 `source`，用对象包着方便共享和修改。

**Q4：`slice(length)` 为什么相当于"砍掉 length 个字符"？**

A：字符串索引 0 到 length-1 正好是 length 个字符。从索引 `length` 开始截，前面 length 个字符被跳过了。

**Q5：`advanceBy` 的第二个参数是"要砍掉多少个字符"还是"截到哪个索引"？**

A：是"要砍掉多少个字符"。传 `content.length` 比传 `endIndex` 更符合语义。

**Q6：为什么要砍 source？**

A：让 `source` 永远只保留"还没解析的部分"，每次只看开头就行，不用维护额外的位置指针。

---

## 📊 自查清单

| 题号 | 问题 | 能回答？ |
|---|---|---|
| 1 | `createParserContext` 是干什么的？ | ☐ |
| 2 | `advanceBy` 是干什么的？ | ☐ |
| 3 | 为什么用对象包着 source？ | ☐ |
| 4 | `slice(length)` 为什么相当于砍？ | ☐ |
| 5 | `advanceBy` 的第二个参数是什么意思？ | ☐ |
| 6 | 为什么要砍 source？ | ☐ |

---

这块清楚了吗？清楚了我们进下一块。

---

## 场景四：用户说"启动不了"

**用户**：我不想学，帮我开始

**AI**：

好，我们一步一步来。

**第 1 步**：现在，什么都不用做，就回答我一个问题——你现在的手机，是拿在手里，还是放在旁边？

（等用户回答。）

（用户回答"手里"后）

**第 2 步**：把手机放下，放到你手够不到的地方。放好了，回我一句「放好了」。

（等用户回答。）

（用户回答"好了"后）

**第 3 步**：坐起来，或者换个姿势，从躺变成坐。不用下床，靠着床头坐起来就行。坐好了，回我一句「坐好了」。

（继续一步一步，直到用户进入学习状态。）

---

## 场景五：用户说"帮我定个学习计划"

**用户**：帮我定个学习计划，我 14 天后面试前端岗，工作日每天能学 2 小时，周末 5 小时。八股只背过一点 Vue，算法刷过十几道，项目能讲流程但讲不清难点。

**AI**：

三个信息都齐了（倒计时 14 天、每日时长、三围基础），直接出计划。

---

## ⏰ 倒计时看板

- 面试日期：2026-10-05（周一）
- 距面试还有：**14 天**（今天 9/21 周一，是第 1 天 / 共 14 天）
- 可用总时长：约 **36 小时**（工作日 2h × 10 天 ＋ 周末 5h × 4 天 ＝ 40h，已扣 10% 弹性）
- 今日可学：2 小时 ＝ 3 个学习块（+复习、打卡各 10 分钟）

---

## 📊 三围诊断与时间配比

| 维度 | 当前基础 | 档位 | 时间占比 |
|---|---|---|---|
| 八股 | 只背过一点 Vue，JS / 网络 / 工程化没开始 | 弱 | 35% |
| 算法 | 刷过十几道，没成体系 | 弱 | 40% |
| 项目 | 能讲流程，难点和自己的贡献讲不深 | 中 | 25% |

**打法**：14 天重点突破——八股只背高频（JS 优先，Vue 捡你背过的加深），算法主攻 Hot100 高频题型，项目准备 3 个 STAR 故事。冷门章节直接放弃。

---

## 🗺️ 三阶段总览

| 阶段 | 日期 | 天数 | 主攻 | 关键产出 |
|---|---|---|---|---|
| 打底 / 补弱 | 9/21 - 9/27 | 7 天 | JS 高频八股一轮；Hot100 数组/哈希/链表；第 1 个项目 STAR | JS 12 题能脱稿讲；20 道题过完；STAR v1 |
| 强化 | 9/28 - 10/1 | 4 天 | Vue / HTTP / 工程化高频；双指针/滑窗/二叉树；项目难点 + 手写题 | 3 个 STAR 成型；各题型模板能默写 |
| 冲刺 | 10/2 - 10/4 | 3 天 | 错题过两遍 + 2 次模拟面试 + 项目逐字稿，**不学新内容** | 模拟面试走完两轮 |

---

## 📋 今日任务（第 1 天 · 9 月 21 日 · 周一）

| 块 | 时长 | 内容 | 材料 / 范围 | 产出标准（能验收） | 打卡 |
|---|---|---|---|---|---|
| 0 | 10 min | 复习：第一天无，直接开始 | - | - | ☐ |
| 1 | 35 min | 算法：Hot100 数组起步，1.两数之和（哈希）+ 283.移动零（双指针） | LeetCode Hot100 | 两题都能不看答案独立写出，能讲清双指针为什么不漏解 | ☐ |
| 2 | 40 min | 八股：JS 高频之王——闭包 | 用「背八股」四段式 | 能讲清闭包定义/用途/坑，能现场手写一个防抖 | ☐ |
| 3 | 30 min | 项目：挑简历上最熟的 1 个项目，写 STAR 骨架，列 3 个可挖难点 | 自己的简历 | 产出一段 200 字 STAR + 3 个难点标题 | ☐ |
| 4 | 10 min | 回顾打卡 | - | 回报完成几块、卡在哪 | ☐ |

**今日唯一目标**：两道算法能独立写出来，闭包能脱稿讲。项目骨架写不完可以顺延，但前两件必须完成。

---

## 📅 本周剩余安排

| 日期 | 主题 | 块数 |
|---|---|---|
| 9/22 周二 | 算法：哈希表 2 题；八股：this 指向 + 作用域链 | 3 |
| 9/23 周三 | 算法：滑动窗口（3.无重复最长子串）；八股：原型链 + new | 3 |
| 9/24 周四 | 算法：链表反转类（206 / 21）；八股：EventLoop + Promise | 3 |
| 9/25 周五 | 算法：本周错题重做；八股：异步专题 + 手写一道 | 3 |
| 9/26 周六 | 算法 8 题综合；JS 章节整复习；细化第 1 个 STAR | 7 |
| 9/27 周日 | 第 1 周自测（里程碑①）；Vue 响应式高频起步；第 2 个 STAR | 7 |

（每天只细排当天。想提前看明天的细节，说"排明天的任务"。）

---

## 🚩 里程碑检查点

- **第 7 天（9/27）**：JS 高频 12 题能脱稿讲；数组/哈希/链表约 20 题过完且能默写；第 1 个项目 STAR v1。
- **第 11 天（10/1）**：双指针/滑窗/链表/二叉树模板能默写；3 个 STAR 故事成型。
- **第 14 天（10/4）**：完成 2 次模拟面试；所有错题过两遍；项目逐字稿背熟。

---

## 🔧 计划调整规则

- 完不成：连续两天没完成，任务量自动减半，先保证每天不断（算法每天至少 1 道的底线不破）。
- 超前：不赶工，多余时间给算法弱项，或加一次模拟面试。
- 日期变了：告诉我新的面试日期，重算总账、重新排阶段。

---

学完今天回来打卡：**完成了几块、卡在哪**，我按实际情况调整后面的计划。现在从块 1 开始——先做两数之和，写完发我，我帮你批。