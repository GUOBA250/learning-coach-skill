# 项目模板（任务先行块）

> 每个项目块的完整输出格式，配合 `references/rules/任务先行规范.md` 使用。

---

## 第 N 块：xxx（本块只做 1-2 个可观测点）

### ① 词汇卡（2min）

| 词 | 一句话作用 |
|---|---|
| `xxx` | xxx |
| `xxx` | xxx |

（3-5 个本块任务必需的框架/领域词，只给对照不展开；用完即走，不建表存档。）

---

### ② 任务书

- **改哪个文件**：`xxx.js` L18-L24 的 `xxx` 块
- **做什么**：xxx
- **怎么算赢**：（浏览器/运行时里肉眼可见的现象）出现 xxx = 赢

---

### ③ 先预测

动手前先说一句：你猜跑起来会怎样、数据从哪来到哪去？

> 用户预测原话：「xxx」（记下来，不评判，验证后对账。）

---

### ④ 动手验证

（用户自己改、自己点；卡住超 5 分钟才给最小提示，不替他写。回来报告现象。）

---

### ⑤ 机制收口（2-3min，只讲刚踩过的）

**🔴 L1**：一句可复述的标准说法——"xxx"。

（没在任务里踩到、但大纲要求的机制点，单独用现象倒推法练：给一个现象 → 让用户倒推框架背后做了什么。）

**物证验收**：对着刚才的现象（页面 / log / 时序图）把因果链讲一遍——"出现这个现象，是因为 xxx → 又导致 xxx"。讲得清因果链即过，不要求空手背原话；讲不清不打 0 分，下一块换个现象再倒推。

---

### ⑥ 词汇收尾

- `xxx`：补一句"刚在 `xxx.js` L18 亲手改过"，现场过一遍。
- 本块没亲手用到的词不收。

---

这个点通了我们进下一块。下一块的可观测点：xxx。
